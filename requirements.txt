# Crypto Trading Agent — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Exchange SDKs ──────────────────────────────────────
hyperliquid-python-sdk>=0.22.0   # Hyperliquid DEX
lighter-sdk                       # Lighter DEX (zkSync)
eth-account>=0.11.0               # EVM wallet signing

# ── Data & maths ──────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# ── Config ────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Dashboard / local control plane ───────────────────
flask>=3.0.0
gunicorn>=21.2.0

# ── Market data ───────────────────────────────────────
yfinance>=0.2.36                  # Yahoo Finance (SP500 / index data)

# ── SSL / HTTPS (production) ──────────────────────────
certifi>=2024.2.2                 # Up-to-date CA bundle for cloud HTTPS
urllib3>=2.0.0

# ── Scheduling / process management ──────────────────
# (no extra deps needed — agent uses time.sleep loop)

# ── Optional: faster HTTP (nice-to-have) ──────────────
# httpx>=0.27.0
